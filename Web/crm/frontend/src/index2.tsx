import React from "react";
import ReactDOM from "react-dom/client";
import DonationsPage from "./Donations_page";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DonationsPage />
  </React.StrictMode>,
);
